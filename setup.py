from setuptools import setup, find_packages

setup(
    name='bet_on_sibyl',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'colorama',
        'matplotlib'
    ]
)
